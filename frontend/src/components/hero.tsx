import Section from "@/components/section";

interface SectionProps {
    id: string;
}
const Hero = ({id}: SectionProps) => {
    return (
        <div>
            <h1>Hero</h1>
        </div>
    )
}

export default Hero