interface ServiceType {
    image: string;
    title: string;
    text: string;
}

interface Service_blockType {
    services: ServiceType[];
    link: string;
    catchPhrase: string;
    text: string;
    thumbnail: string;
}